// import { fromJS } from 'immutable';
// import { makeSelectSearchPageDomain } from '../selectors';

// const selector = makeSelectSearchPageDomain();

describe('makeSelectSearchPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

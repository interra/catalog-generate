// import { fromJS } from 'immutable';
// import { makeSelectCollectionPageDomain } from '../selectors';

// const selector = makeSelectCollectionPageDomain();

describe('makeSelectCollectionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

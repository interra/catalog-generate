import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  max-width: 150px;
  margin: 0 auto;
  padding: 10px 0;
  float: left;
  display: block;
`;

export default Img;
